apiVersion: apps/v1
kind: Deployment
metadata:
  name: retailer-deploy
  namespace: vkpaints-dev-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: retailer-service
  template:
    metadata:
      labels:
        app: retailer-service
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                app: retailer-service
            topologyKey: "kubernetes.io/hostname"
      containers:
      - name: retailer-container
        image: ghcr.io/vk-paints/vkpaints-retailer-service:v1.0.0
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 128Mi
        envFrom:
        - configMapRef:
            name: retailer-config
        livenessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 5
          periodSeconds: 10

        ports:
        - containerPort: 3000

